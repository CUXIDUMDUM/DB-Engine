$ bin/mysql.pl
Available commands:

  commands: list the application's commands
      help: display a command's help screen

  createdb: Create a MYSQL database
    dropdb: Drop a MYSQL database
    dumpdb: Dump MYSQL databases(s) to a directory

$ bin/mysql.pl help
Available commands:

  commands: list the application's commands
      help: display a command's help screen

  createdb: Create a MYSQL database
    dropdb: Drop a MYSQL database
    dumpdb: Dump MYSQL databases(s) to a directory

$ bin/mysql.pl help createdb
Mandatory parameter 'database' missing in call to (eval)

mysql.pl createdb [-?h] [long options...]
	--database           Name of the database to be created. Mandatory
	                     option.
	-h -? --usage --help  Prints this usage information.
	--user               mysql user
	--password           mysql password
	--host               mysql host
	--log4perl_conf      config file for Log::Log4perl. default
	                     etc/log4perl.conf
	--configfile         yaml config file to specify all command line
	                     options
	--port               mysql port

$ bin/mysql.pl help dropdb
Mandatory parameter 'database' missing in call to (eval)

mysql.pl dropdb [-?h] [long options...]
	--database           Name of the database to be dropped. Mandatory
	                     Option.
	-h -? --usage --help  Prints this usage information.
	--user               mysql user
	--password           mysql password
	--host               mysql host
	--configfile         yaml config file to specify all command line
	                     options
	--port               mysql port
	--log4perl_conf      config file for Log::Log4perl. default
	                     etc/log4perl.conf

$ bin/mysql.pl help dumpdb
mysql.pl dumpdb [-?h] [long options...]

	--databases          List of databases to be dumped. To be used as
	                     --database db1 --database db2.
	--dump_dir           Base directory where all database would be
	                     dumped to
	-h -? --usage --help  Prints this usage information.
	--user               mysql user
	--password           mysql password
	--host               mysql host
	--port               mysql port
	--log4perl_conf      config file for Log::Log4perl. default
	                     etc/log4perl.conf
	--configfile         yaml config file to specify all command line
	                     options

